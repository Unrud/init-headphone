This directory contains a configuration for **pm-utils**  to run the script
after suspend and hibernation. This is unnecessary if **systemd** is used.

Copy the file `init-headphone` to `/etc/pm/sleep.d`.