This directory contains a configuration for **Upstart** to run the script on
boot. You can use **pm-utils** to run the script after suspend and
hibernation.

Copy the file `init-headphone.conf` to `/etc/init`.